[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "ASYNC"]
extend-select = ["B"]
# 忽略行长度检查（已在 line-length 中统一配置）
# 忽略 ASYNC109（建议使用 asyncio.timeout 而非 timeout 参数）
# asyncio.timeout 是 Python 3.11+ 特性，我们使用 asyncio.wait_for 以兼容 Python 3.7+
ignore = ["E501", "ASYNC109"]

[tool.ruff.lint.per-file-ignores]
# 针对特定文件可以放宽限制

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.isort]
line_length = 100
multi_line_output = 3
include_trailing_comma = false
force_grid_wrap = 0
use_parentheses = true
